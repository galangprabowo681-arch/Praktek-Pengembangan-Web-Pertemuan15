<!DOCTYPE html>
<html>
<head>
  <title>Edit Barang</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

<div class="container mt-4">
  <h4>✏️ Edit Barang</h4>

  <form action="/items/update/<%= item.id %>" method="POST">
    <input name="item_name" value="<%= item.item_name %>" class="form-control mb-2" required>
    <input name="category" value="<%= item.category %>" class="form-control mb-2" required>
    <input name="price" type="number" value="<%= item.price %>" class="form-control mb-2" required>
    <input name="stock_qty" type="number" value="<%= item.stock_qty %>" class="form-control mb-2" required>
    <input name="min_stock" type="number" value="<%= item.min_stock %>" class="form-control mb-2" required>
    <input name="storage_location" value="<%= item.storage_location %>" class="form-control mb-3" required>

    <button class="btn btn-warning">Update</button>
    <a href="/dashboard" class="btn btn-secondary">Kembali</a>
  </form>
</div>

</body>
</html>
